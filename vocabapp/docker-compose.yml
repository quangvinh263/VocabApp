version: '3.8'
services:
  db:
    image: postgres:15-alpine
    container_name: vocab_postgres
    environment:
      POSTGRES_USER: vinhnguyen
      POSTGRES_PASSWORD: quangvinh263
      POSTGRES_DB: vocab_db
    ports:
      - "5433:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: